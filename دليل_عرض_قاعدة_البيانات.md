# 📊 دليل عرض قاعدة بيانات PredictIQ

## 🚀 الطريقة السريعة (موصى بها)

### استخدام سكريبت Python:
```bash
# عرض جميع البصمات
python backend/view_database.py

# عرض الإحصائيات فقط
python backend/view_database.py --stats

# عرض بصمات مستخدم معين
python backend/view_database.py --user user-8456123848

# تصدير البيانات إلى JSON
python backend/view_database.py --export fingerprints.json
```

### أو استخدام ملف BAT:
```bash
view_database.bat
```

## 🛠️ طرق أخرى لعرض قاعدة البيانات

### 1. DB Browser for SQLite (أداة رسومية)

**التحميل:** https://sqlitebrowser.org/

**الاستخدام:**
1. افتح DB Browser for SQLite
2. اضغط "Open Database"
3. اختر الملف: `predictiq.db`
4. ستجد الجدول `fingerprints` في الجانب الأيسر

**المميزات:**
- عرض البيانات في جدول
- إمكانية البحث والتصفية
- تعديل البيانات يدوياً
- تصدير البيانات إلى CSV/JSON

---

### 2. Python Script (الذي أنشأناه)

**العرض الكامل:**
```bash
python backend/view_database.py
```

**الإحصائيات:**
```bash
python backend/view_database.py --stats
```

**البحث عن مستخدم:**
```bash
python backend/view_database.py --user user-8456123848
```

**التصدير:**
```bash
python backend/view_database.py --export my_export.json
```

---

### 3. SQLite Command Line

**إذا كان sqlite3 مثبت:**
```bash
sqlite3 predictiq.db
```

**أوامر مفيدة:**
```sql
-- عرض جميع الجداول
.tables

-- عرض هيكل جدول fingerprints
.schema fingerprints

-- عرض جميع البصمات
SELECT * FROM fingerprints;

-- عرض البصمات النشطة فقط
SELECT fingerprint_id, user_id, risk_score, status 
FROM fingerprints 
WHERE status = 'ACTIVE';

-- عد البصمات حسب الحالة
SELECT status, COUNT(*) 
FROM fingerprints 
GROUP BY status;

-- الخروج
.exit
```

---

### 4. Python Interactive Console

```python
from backend.db import get_db_session, FingerprintDB

session = get_db_session()
fp = session.query(FingerprintDB).first()
print(fp.fingerprint_id, fp.user_id, fp.risk_score, fp.status)
session.close()
```

---

### 5. عبر API (عرض في Dashboard)

**افتح في المتصفح:**
```
http://localhost:5000/dashboard.html
```

أو

```
http://localhost:5000/soc-admin-dashboard.html
```

**أو API مباشرة:**
```
http://localhost:5000/api/v1/fingerprints
```

---

## 📋 المعلومات المعروضة

عند استخدام السكريبت، ستحصل على:

- ✅ معرف البصمة (fingerprint_id)
- ✅ معرف المستخدم (user_id)
- ✅ معرف الجهاز (device_id)
- ✅ عنوان IP
- ✅ User Agent
- ✅ درجة الخطر (risk_score)
- ✅ الحالة (status: ACTIVE/BLOCKED/CLEARED)
- ✅ تاريخ الإنشاء والتحديث
- ✅ الخصائص السلوكية (behavioral_features)
- ✅ البصمات المشابهة (related_fingerprints)

---

## 🔍 أمثلة استخدام

### مثال 1: عرض جميع البصمات
```bash
python backend/view_database.py
```

### مثال 2: عرض إحصائيات سريعة
```bash
python backend/view_database.py --stats
```

### مثال 3: البحث عن مستخدم محدد
```bash
python backend/view_database.py --user user-8456123848
```

### مثال 4: تصدير جميع البيانات
```bash
python backend/view_database.py --export backup_2024.json
```

---

## 💡 نصائح

1. **DB Browser for SQLite** - أفضل طريقة للعرض المرئي
2. **Python Script** - أفضل طريقة للعرض السريع والإحصائيات
3. **Dashboard** - أفضل طريقة لمتابعة التحديثات في الوقت الفعلي
4. **SQLite CLI** - أفضل طريقة للمستخدمين المتقدمين

---

## 🐛 حل المشاكل

### خطأ: "No module named 'db'"
```bash
cd backend
python view_database.py
```

### خطأ: "Database file not found"
- تأكد من وجود ملف `predictiq.db` في المجلد الرئيسي
- أو شغّل السيرفر مرة واحدة لإنشاء قاعدة البيانات

---

**جاهز للاستخدام! 🎉**

