# 📊 دليل عرض قاعدة بيانات PredictIQ

## 🚀 الطريقة السريعة

### Windows:
```bash
view_database.bat
```

### أو مباشرة:
```bash
.venv\Scripts\python.exe backend\view_database.py
```

---

## 📋 الخيارات المتاحة

### 1. عرض جميع البصمات (افتراضي):
```bash
python backend/view_database.py
```
يعرض:
- جميع البصمات مع تفاصيلها الكاملة
- الخصائص السلوكية
- البصمات المشابهة
- التواريخ والأوقات

---

### 2. عرض الإحصائيات فقط:
```bash
python backend/view_database.py --stats
```
يعرض:
- إجمالي البصمات
- التوزيع حسب الحالة (ACTIVE/BLOCKED/CLEARED)
- التوزيع حسب درجة الخطر
- متوسط درجة الخطر

---

### 3. البحث عن مستخدم محدد:
```bash
python backend/view_database.py --user user-8456123848
```
يعرض جميع بصمات المستخدم المحدد

---

### 4. تصدير البيانات إلى JSON:
```bash
python backend/view_database.py --export backup.json
```
يصدر جميع البيانات إلى ملف JSON قابل للقراءة

---

## 🛠️ طرق أخرى

### DB Browser for SQLite (GUI)

1. **تحميل من:** https://sqlitebrowser.org/
2. **فتح الملف:** `predictiq.db`
3. **عرض الجدول:** `fingerprints`

**مميزات:**
- واجهة رسومية سهلة
- بحث وتصفية
- تعديل البيانات
- تصدير إلى CSV/JSON

---

### عبر Dashboard (في المتصفح)

**لوحة التحكم:**
```
http://localhost:5000/dashboard.html
```

**لوحة SOC:**
```
http://localhost:5000/soc-admin-dashboard.html
```

**API مباشر:**
```
http://localhost:5000/api/v1/fingerprints
```

---

### SQLite Command Line

```bash
sqlite3 predictiq.db

# عرض جميع البصمات
SELECT * FROM fingerprints;

# عرض البصمات النشطة
SELECT fingerprint_id, user_id, risk_score 
FROM fingerprints 
WHERE status = 'ACTIVE';
```

---

## 📍 موقع قاعدة البيانات

**المسار الكامل:**
```
C:\Users\waaad\OneDrive - ek.com.sa\Documents\hakathoon\predictiq.db
```

---

## 💡 أمثلة

### مثال 1: عرض كامل
```
view_database.bat
```

### مثال 2: إحصائيات سريعة
```
view_database.bat --stats
```

### مثال 3: بحث مستخدم
```
view_database.bat --user user-8456123848
```

---

## ✅ تم إنشاء الأدوات التالية:

1. ✅ `backend/view_database.py` - السكريبت الرئيسي
2. ✅ `view_database.bat` - ملف تشغيل سريع
3. ✅ `دليل_عرض_قاعدة_البيانات.md` - دليل مفصل

**جاهز للاستخدام! 🎉**

