# دليل اختبار خصائص PredictIQ

## 🧪 اختبار جميع الخصائص

تم إنشاء ملف اختبار شامل لاختبار جميع ميزات النظام.

## 📋 الاختبارات المتوفرة

### 1. **تهيئة قاعدة البيانات**
- اختبار إنشاء الجداول تلقائياً
- التحقق من الاتصال بقاعدة البيانات

### 2. **تخزين البصمات الأساسي**
- اختبار حفظ بصمة في قاعدة البيانات
- اختبار استرجاع البصمات

### 3. **اكتشاف الحسابات المتعددة (Multi-Account Detection)**
- اختبار اكتشاف نفس المهاجم يستخدم حسابات مختلفة
- نفس الجهاز/IP مع أنماط سلوكية مشابهة

### 4. **اكتشاف تبديل المتصفحات (Browser-Hopping Detection)**
- اختبار اكتشاف التبديل السريع بين المتصفحات
- 3+ متصفحات مختلفة في 60 ثانية

### 5. **اكتشاف السلوك المشابه (Similar-Behavior Detection)**
- اختبار البحث عن البصمات المشابهة تاريخياً
- استخدام cosine similarity لمقارنة الأنماط

### 6. **زيادة درجة الخطر من التشابه**
- اختبار رفع risk_score عند اكتشاف بصمة مشابهة لمحظورة
- التحقق من ربط البصمات المشابهة

### 7. **عمليات قاعدة البيانات**
- تحديث حالة البصمة
- حذف البصمة
- مسح بصمات مستخدم

## 🚀 كيفية التشغيل

### Windows:
```bash
test_all_features.bat
```

أو مباشرة:
```bash
python backend\test_features.py
```

### Linux/Mac:
```bash
chmod +x test_all_features.sh
./test_all_features.sh
```

أو مباشرة:
```bash
python3 backend/test_features.py
```

## 📊 النتائج المتوقعة

عند تشغيل الاختبارات، سترى:

```
✅ PASS - Database Initialization
✅ PASS - Basic Fingerprint Storage
✅ PASS - Multi-Account Detection
✅ PASS - Browser-Hopping Detection
✅ PASS - Similar-Behavior Detection
✅ PASS - Risk Score Boost
✅ PASS - Database Operations

Total: 7/7 tests passed
```

## 🔍 ما يتم اختباره بالتفصيل

### اختبار Multi-Account:
1. إنشاء بصمة للمستخدم الأول (`attacker_account_1`)
2. إنشاء حدث لمستخدم مختلف (`attacker_account_2`) بنفس الجهاز/IP
3. التحقق من اكتشاف الهجوم ورفع risk_score إلى 95+

### اختبار Browser-Hopping:
1. إنشاء 3 أحداث بمتصفحات مختلفة في 60 ثانية
2. التحقق من اكتشاف التبديل ورفع risk_score إلى 90+

### اختبار Similar-Behavior:
1. إنشاء بصمة محظورة بنمط معين
2. إنشاء حدث جديد بنمط مشابه
3. التحقق من اكتشاف التشابه وربط البصمات

## 🧹 تنظيف البيانات

بعد الانتهاء من الاختبارات، يمكنك اختيار حذف البيانات التجريبية:
- سيتم حذف جميع البصمات التي تبدأ بـ `fp-test-`, `fp-multi-`, `fp-blocked-`, `fp-db-test-`

## ⚠️ ملاحظات

- تأكد من تثبيت جميع المكتبات: `pip install -r requirements.txt`
- قاعدة البيانات (`predictiq.db`) سيتم إنشاؤها تلقائياً
- الاختبارات لا تؤثر على البيانات الحقيقية (يمكن حذفها بعد الاختبار)

## 🐛 استكشاف الأخطاء

إذا فشل أي اختبار:
1. تحقق من تثبيت SQLAlchemy: `pip install SQLAlchemy`
2. تحقق من وجود ملف `predictiq.db` في مجلد المشروع
3. تأكد من تشغيل `init_db()` مرة واحدة على الأقل
