# 🧪 دليل اختبار خصائص PredictIQ

## كيفية تجربة جميع الخصائص

تم إنشاء ملف اختبار شامل (`backend/test_features.py`) لاختبار جميع الميزات.

## 🚀 التشغيل السريع

### Windows:
```bash
test_all_features.bat
```

### Linux/Mac:
```bash
./test_all_features.sh
```

أو مباشرة:
```bash
python backend/test_features.py
```

## 📋 الاختبارات المتوفرة

### ✅ 1. تهيئة قاعدة البيانات
- إنشاء الجداول تلقائياً
- التحقق من الاتصال

### ✅ 2. تخزين البصمات
- حفظ بصمة في قاعدة البيانات
- استرجاع البصمات

### ✅ 3. اكتشاف الحسابات المتعددة
- نفس المهاجم يستخدم حسابات مختلفة
- نفس الجهاز/IP مع أنماط مشابهة

### ✅ 4. اكتشاف تبديل المتصفحات
- 3+ متصفحات مختلفة في 60 ثانية
- رفع risk_score إلى 90+

### ✅ 5. اكتشاف السلوك المشابه
- البحث عن بصمات مشابهة تاريخياً
- استخدام cosine similarity

### ✅ 6. زيادة درجة الخطر
- رفع risk_score عند اكتشاف بصمة مشابهة لمحظورة
- ربط البصمات المشابهة

### ✅ 7. عمليات قاعدة البيانات
- تحديث/حذف/مسح البصمات

## 📊 مثال على النتائج

```
╔════════════════════════════════════════════════════════════╗
║         PredictIQ - Feature Testing Suite                 ║
╚════════════════════════════════════════════════════════════╝

============================================================
TEST 1: Database Initialization
============================================================
✅ Database initialized successfully
ℹ️  Current fingerprints in database: 0

============================================================
TEST 2: Basic Fingerprint Storage
============================================================
✅ Fingerprint stored: fp-test-001
✅ Fingerprint retrieved: fp-test-001, Risk: 85

...

============================================================
Test Results Summary
============================================================
✅ PASS - Database Initialization
✅ PASS - Basic Fingerprint Storage
✅ PASS - Multi-Account Detection
✅ PASS - Browser-Hopping Detection
✅ PASS - Similar-Behavior Detection
✅ PASS - Risk Score Boost
✅ PASS - Database Operations

Total: 7/7 tests passed
```

## 🔍 تفاصيل الاختبارات

### اختبار Multi-Account:
1. إنشاء بصمة للمستخدم `attacker_account_1`
2. إنشاء حدث لمستخدم مختلف `attacker_account_2` بنفس الجهاز/IP
3. ✅ يجب اكتشاف الهجوم ورفع risk_score إلى 95+

### اختبار Browser-Hopping:
1. إنشاء 3 أحداث بمتصفحات مختلفة في 60 ثانية
2. ✅ يجب اكتشاف التبديل ورفع risk_score إلى 90+

### اختبار Similar-Behavior:
1. إنشاء بصمة محظورة بنمط معين
2. إنشاء حدث جديد بنمط مشابه (≥70% تشابه)
3. ✅ يجب اكتشاف التشابه وربط البصمات
4. ✅ إذا كانت البصمة المشابهة محظورة/نشطة، يجب رفع risk_score إلى 90+

## 🧹 تنظيف البيانات

بعد الانتهاء، يمكنك حذف البيانات التجريبية:
- سيتم حذف البصمات التي تبدأ بـ `fp-test-`, `fp-multi-`, `fp-blocked-`, `fp-db-test-`

## ⚠️ متطلبات التشغيل

1. تثبيت المكتبات:
```bash
pip install -r requirements.txt
```

2. قاعدة البيانات:
- سيتم إنشاء `predictiq.db` تلقائياً
- لا حاجة لإعداد يدوي

## 🐛 حل المشاكل

### خطأ: "Import sqlalchemy"
```bash
pip install SQLAlchemy
```

### خطأ: "Database not initialized"
- تأكد من تشغيل `init_db()` مرة واحدة
- تحقق من وجود ملف `predictiq.db`

### خطأ: "No module named 'db'"
- تأكد من تشغيل السكريبت من المجلد الرئيسي
- أو استخدم: `python -m backend.test_features`

## 📝 ملاحظات

- الاختبارات لا تؤثر على البيانات الحقيقية
- يمكن حذف البيانات التجريبية بعد الاختبار
- جميع الاختبارات مستقلة ويمكن تشغيلها بشكل منفصل

